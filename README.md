# 🎓 Assessment Question Difficulty Classifier

[![Tests and Code Quality](https://github.com/Lanzuelo/VideProject/actions/workflows/tests.yml/badge.svg)](https://github.com/Lanzuelo/VideProject/actions/workflows/tests.yml)
[![Train Model](https://github.com/Lanzuelo/VideProject/actions/workflows/train_ml.yml/badge.svg)](https://github.com/Lanzuelo/VideProject/actions/workflows/train_ml.yml)
![Python](https://img.shields.io/badge/python-3.9%20%7C%203.10-blue)
![License](https://img.shields.io/badge/license-MIT-green)

## 📖 О проекте

**Assessment Question Difficulty Classifier** — это система машинного обучения, предназначенная для автоматического определения уровня сложности (Easy, Medium, Hard) учебных вопросов.

В современных образовательных платформах (LMS) важно балансировать сложность тестов. Ручная разметка тысяч вопросов занимает много времени. Этот проект решает проблему с помощью AI, анализируя текст вопроса и исторические данные ответов.

### ✨ Ключевые возможности
* 🧠 **NLP Анализ:** Оценка сложности текста (метрика Flesch-Kincaid).
* 📊 **Учет статистики:** Использование среднего времени ответа и процента правильных решений.
* 🎲 **Генератор данных:** Встроенный модуль для создания реалистичных синтетических датасетов.
* 🔄 **ML Ops:** Полностью автоматизированный пайплайн обучения через GitHub Actions.

---

## ⚙️ Как это работает?

Модель принимает на вход сырые данные вопроса и преобразует их в предсказание.

### 1. Входные данные (Dataset)
Система обучается на CSV-файле со следующими колонками:

| Колонка | Описание | Пример |
| :--- | :--- | :--- |
| `question_text` | Текст самого вопроса | "Calculate the derivative of x^2..." |
| `avg_time_seconds` | Среднее время, затраченное студентами (сек) | `45.5` |
| `success_rate` | Процент правильных ответов (0.0 - 1.0) | `0.75` (75%) |
| `topic` | Тема вопроса (для информации) | "Math" |
| `difficulty` | **Целевая метка (Target)** | `Medium` |

### 2. Feature Engineering (Извлечение признаков)
Перед подачей в модель данные обогащаются:
* **Flesch Reading Ease:** Лингвистическая метрика, показывающая, насколько сложно читать текст.
* **Word Count:** Длина вопроса (длинные вопросы часто сложнее).

### 3. Модель
Используется **Random Forest Classifier** внутри Pipeline. Текстовые данные векторизуются через `TfidfVectorizer`, числовые — нормализуются через `StandardScaler`.

---

## 🛠 Установка и запуск

### Предварительные требования
* Python 3.8+
* Git

### Пошаговая инструкция

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/Lanzuelo/VideProject.git](https://github.com/Lanzuelo/VideProject.git)
    cd VideProject
    ```

2.  **Создайте виртуальное окружение:**
    *Windows:*
    ```powershell
    python -m venv venv
    .\venv\Scripts\activate
    ```
    *Linux/macOS:*
    ```bash
    python3 -m venv venv
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

---

## 🚀 Примеры использования

### Шаг 1: Генерация данных
Если у вас нет своих данных, сгенерируйте их (200 примеров по умолчанию):

```bash
python src/data_gen.py
```

**Результат:**
> ✅ Dataset generated at data/dataset.csv

### Шаг 2: Обучение модели
Запустите скрипт обучения. Он автоматически прочитает данные, обучит модель и выведет отчет.

```bash
python src/model.py
```

**Примеры вывода в консоли**

Model Score: 0.98
precision    recall  f1-score   support

        Easy       1.00      1.00      1.00        15
        Hard       0.92      1.00      0.96        12
      Medium       1.00      0.92      0.96        13

    accuracy                           0.97        40
macro avg       0.97      0.97      0.97        40
weighted avg       0.98      0.97      0.97        40

В папке проекта появится файл model.pkl (готовая модель) и metrics.txt.

## Шаг 3: Тестирование
Для проверки качества кода запустите тесты:

```bash pytest tests/ -v```

## 🤖 CI/CD и Автоматизация

Проект использует **GitHub Actions** для реализации практик DevOps.

### 1. `tests.yml` (Quality Gate)
* **Триггер:** Push или Pull Request в ветку `main`.
* **Что делает:**
   * Проверяет стиль кода (`flake8`).
   * Запускает unit-тесты (`pytest`).
   * Гарантирует, что сломанный код не попадет в продакшн.

### 2. `train_ml.yml` (Continuous Training)
* **Триггер:** Push в `main` (или ручной запуск).
* **Креативная функция:**
  Это workflow реализует концепцию **CT (Continuous Training)**. При изменении кода сервер GitHub автоматически:
   1. Генерирует свежий набор данных.
   2. Переобучает модель "с нуля".
   3. Сохраняет файл с метриками (`metrics.txt`) как **Artifact**.

  > *Вы можете скачать отчет о точности модели прямо из вкладки Actions на GitHub, не запуская код локально.*

## 📂 Структура проекта

```text
VideProject/
├── .github/workflows/      # CI/CD конфигурации
│   ├── tests.yml           # Тестирование и линтинг
│   └── train_ml.yml        # Авто-обучение модели
├── data/                   # Хранилище данных
├── src/                    # Исходный код
│   ├── __init__.py
│   ├── data_gen.py         # Скрипт генерации синтетики
│   └── model.py            # Класс модели и пайплайн обучения
├── tests/                  # Unit-тесты
│   ├── __init__.py
│   └── test_model.py       # Тесты логики обучения
├── .gitignore              # Исключения git
├── requirements.txt        # Список библиотек
└── README.md               # Документация
```

## ‍💻 Автор
**Project by Lanzuelo**